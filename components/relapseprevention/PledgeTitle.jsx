import React from 'react'
import { Text } from 'react-native'

import styles from './relapseprevention.style'

const PledgeTitle = () => {
  return (
    <Text style={styles.pledgeTitle}>
      Today's pledge:
    </Text>
  )
}

export default PledgeTitle 